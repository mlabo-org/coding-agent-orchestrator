interface:
  display_name: "Coding Agents"
  short_description: "State-first Coding Agents continuation"
  default_prompt: "Use $coding-agents to inspect this valid .coding-agents state, semantically resume related work with completed progress preserved, and use fresh intake only for clearly unrelated work without asking me for a mode."
policy:
  allow_implicit_invocation: true
