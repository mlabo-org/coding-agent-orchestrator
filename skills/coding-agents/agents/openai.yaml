interface:
  display_name: "Coding Agents"
  short_description: "State-first Coding Agents continuation"
  default_prompt: "Use $coding-agents to inspect this valid .coding-agents state, semantically resume related work with completed progress preserved, use fresh intake only for clearly unrelated work without asking me for a mode, and default coding/source work to ITERATIVE_DELIVERY; use task-local ONE_SHOT_QUALITY only when I explicitly requested it."
policy:
  allow_implicit_invocation: true
