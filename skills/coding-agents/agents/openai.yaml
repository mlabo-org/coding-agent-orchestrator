interface:
  display_name: "Coding Agents"
  short_description: "Continue same-repo Coding Agents tasks"
  default_prompt: "Use $coding-agents to continue this valid .coding-agents repository for a next stage or new purpose with a fresh task_id, epoch, and scope while preserving runner history."
policy:
  allow_implicit_invocation: true
