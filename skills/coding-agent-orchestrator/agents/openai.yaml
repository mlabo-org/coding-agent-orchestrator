interface:
  display_name: "Coding Agent Orchestrator"
  short_description: "Official subagents with deterministic .CAO state"
  default_prompt: "Use $coding-agent-orchestrator for this coding objective: inspect or initialize .CAO state, record dynamic-role assignments and results, and run workers only through official Codex subagents."
policy:
  allow_implicit_invocation: true
